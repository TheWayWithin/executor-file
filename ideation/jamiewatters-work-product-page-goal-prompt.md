# jamiewatters.work — add Executor File product page: goal prompt

Generated 17 Jul 2026 by /goal-prompt (in the executor-file session, from a
read-only exploration of the JamieWatters repo).

**Suitability verdict:** good fit for goal-first — one repo, one feature, spec
fully stated, constraints verified against the codebase. Run on the strongest
model available (Fable 5).

**How to run:** fresh Claude Code session inside
`/Users/jamiewatters/DevProjects/JamieWatters`, paste the block below after
`/goal`. One deliberate exception to full autonomy is built in: the production
database write waits for Jamie's yes.

**Follow-up for later:** when executor-file tags v0.3.0, flip the product's
status BETA → LIVE (one-line change in the DB row).

---

```
Read this whole prompt before touching anything; every repo fact in it was verified against the codebase on 17 Jul 2026 and is not to be re-litigated. Your job: add the product "Executor File" to jamiewatters.work so that it appears on /portfolio and /portfolio/executor-file with honest metrics, every counter on the site is correct, and it is selectable as the project when creating journey blog posts. This page is part of Jamie's public build-in-public record: excellent looks like a portfolio entry indistinguishable in quality from the hand-made ones, where every number and claim is traceable to the executor-file repo and nothing is invented.

You are working in /Users/jamiewatters/DevProjects/JamieWatters (the site lives in website/: Next.js 15 App Router, Prisma + Postgres on Neon, deployed to Netlify from GitHub). Critical architecture facts: products are DATABASE ROWS (Prisma model Project in website/prisma/schema.prisma), not committed files. lib/placeholder-data.ts is legacy seed data and prisma/seed.ts is DESTRUCTIVE (deleteMany on Project/Post/MetricsHistory) — you must NEVER run the seed. The two legitimate write paths are the admin UI (/admin/projects/new, which posts to app/api/admin/projects/route.ts) or an idempotent one-off script using prisma.project.upsert keyed on slug. The public portfolio filter in lib/database.ts getAllProjects() only shows rows whose url starts with "http" and whose status is not ARCHIVED. Portfolio-page counts are computed and need no edits; the HARDCODED counter at app/page.tsx line 147 ("19 products built with AI…") must be bumped to 20. Journey posts link to products relationally (Post.projectId UUID; the admin post form dropdown auto-populates from the projects table), so once the row exists no schema or enum edits are needed — verify this rather than assuming. Also check whether public/data/dashboard.json and the /dashboard consumers are a stale static dump that should reflect the new product, and fix or flag what you find. Read lib/metrics/metric-templates.ts for the exact OPEN_SOURCE customMetrics keys and fill them with real GitHub numbers for TheWayWithin/executor-file (honest zeros beat invented figures). Go hunt for anything else you need: the executor-file repo README and RELEASE-CHECKLIST.md are public at https://github.com/TheWayWithin/executor-file, and gh CLI is available.

The row data (the WHAT — do not change these facts): name "Executor File", slug "executor-file", tagline "Everything your executor will need, in one encrypted file.", description: an encrypted, self-hosted file that lets an executor find every account, asset, and liability a person owns, and know what they wanted done with each one, with no credentials stored and no service dependency; a file, not an app: age encryption plus 2-of-3 Shamir shares, and recovery needs only two standard open-source tools, forever. url and githubUrl: https://github.com/TheWayWithin/executor-file (executorfile.com is secured but its site is not built yet; the GitHub URL is the honest destination today and satisfies the http filter). status BETA (usable, pre-1.0; two human release gates pending — flip to LIVE only when v0.3.0 tags), projectType OPEN_SOURCE, featured false, mrr 0, users 0, launchedAt 2026-07-16, currentPhase "v0.3 built — awaiting release gates (Windows dry run + fire drill)", techStack ["Bash/POSIX sh", "awk", "age (encryption)", "ssss (Shamir)", "Python (optional strict tier)", "GitHub Actions"]. Category: pick FINANCE or WELLBEING and state why. For longDescription, problemStatement, solutionApproach, lessonsLearned, and timelineEvents (if the shape supports events), write from these verified facts only: executors otherwise spend months discovering accounts and assets like old pensions and crypto get lost forever; the solution is one encrypted register plus printed instructions with credentials never stored and no service to die; born 16 Jul 2026 as the winning arm of a goal-first vs agent-led build experiment, v0.2 shipped the same day, v0.3 "Executor Release" built 17 Jul (schema v3, executor triage report, generated two-page printed guide, fire-drill and rotation tooling, Windows recovery via WSL, 115 scripted tests green on macOS and Ubuntu CI), five independent LLM reviews scored it 8.5–9.1/10, and the v0.3.0 tag waits on a Windows dry run by a non-author plus a physical fire drill. Published copy must follow Jamie's voice rules: no leverage/seamless/robust/unlock/empower/ecosystem/delve/utilize/navigate-as-metaphor, and no em-dashes in site-published prose (recast with colons or periods), British English.

Within that WHAT, the HOW is yours: whether to write the upsert script or use the admin UI, copy structure, category choice, what dashboard.json needs, whether to add an optional logo under public/images/projects/. Make every call yourself and do not defer back to Jamie — with ONE exception: before the production database gains the row, show him the exact final row values and the command you will run, and get a yes. That is the only question you are allowed.

Before calling it done, prove it: run the site locally against the real data path, load /portfolio and /portfolio/executor-file and read the copy as a stranger would, confirm the home page says 20, count DB products before and after your change and show the delta is exactly +1 with all pre-existing rows untouched, open the admin new-post form and confirm Executor File appears in the project dropdown, run the production build (npm run build) clean, and leave behind a small scripted check (a node or shell script under scripts/ that verifies the row exists, the counters match the DB, and the slug page returns 200) so this never regresses silently. Push in reviewable commits and confirm the Netlify deploy is green.

Goal: Executor File live on jamiewatters.work's portfolio with honest metrics, correct counters everywhere, and journey-post selectability, verified end-to-end. Work autonomously — except the single production-write confirmation above, do not ask for anything until it is all done — and parallelise independent work across sub-agents where it helps.
```
